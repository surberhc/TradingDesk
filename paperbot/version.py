"""
version.py — single source of truth for the paperbot system version + change log.

Compliance/traceability: every run STAMPS this version (plus the config knobs and the
per-account model as_of) into its report header and — once order routing exists — into
each audit-ledger record, so any trade decision can be tied back to the exact logic and
settings that produced it. Bump VERSION whenever behavior that can change an order
changes, and add a CHANGELOG line saying WHAT changed and WHY.

This is the lightweight in-code marker. It is NOT a substitute for source-control
history (git) — it complements it. The CHANGELOG here is the human-readable summary;
the repo (when initialized) is the authoritative line-by-line record.
"""
from __future__ import annotations

# 0.MAJOR.MINOR while pre-trading. Bump on ANY change that can affect a generated order
# (strategy wiring, sizing, reserve/band logic, allocation, routing).
VERSION = "0.44.0"

# Newest first. Keep terse; for an examiner the "why" matters as much as the "what".
CHANGELOG = [
    ("0.44.0", "2026-09-01", "THE PLAN-LEVEL 'TOTAL BUY <= INVESTABLE' TRANSMIT GATE NOW "
                             "COMPARES LIKE WITH LIKE. Order-affecting: it removes a "
                             "condition under which the desk refused to trade whole "
                             "accounts. The gate itself is KEPT — only the price BASIS it "
                             "measures on is corrected. "
                             "PROBLEM: the check compared two DIFFERENT price bases. "
                             "rebalance_engine SIZES every position off the REFERENCE price "
                             "(target_shares = int(weight * investable / reference_price)), "
                             "and whole-share rounding is DOWN, so the plan's intended spend "
                             "is strictly BELOW plan.investable by construction. But "
                             "safe_execute.build_deploy_legs builds each leg as qty * the "
                             "MARKETABLE CAP (live_quotes.marketable_cap = ask * (1 + "
                             "config.ORDER_CAP_K), ORDER_CAP_K = 0.003) — the deliberately "
                             "above-market worst-case crossing price that makes the order "
                             "actually fill — and execute_plan summed THOSE notionals and "
                             "compared the total to a reference-basis investable. The gap is "
                             "(ask - reference)/reference plus 30bps, which on a larger "
                             "account exceeds the whole-share rounding slack and refuses the "
                             "ENTIRE account for an overspend that cannot happen: the "
                             "account never pays the cap, it pays the market. "
                             "MEASURED, live read-only preview on the 4003 gateway "
                             "2026-09-01 (after hours, ~51bps spread), scoped to the 14 "
                             "custom-model accounts: THREE of eight sized accounts carried "
                             "this as a second blocking reason that arming would not clear — "
                             "U25274773 (buys at the caps 822,722.51 vs investable "
                             "818,504.60), U27592200 (278,925.36 vs 277,740.44), U26174779 "
                             "(92,736.35 vs 92,606.00). That is $1,194,383 of $1,471,610 — "
                             "81% of the deployment, including the largest account. "
                             "FIX: execute_plan now also computes total_buy_plan, the same "
                             "BUY legs valued at the PLAN's own basis (qty * prices[symbol], "
                             "via the new pure helper safe_execute._leg_plan_notional), and "
                             "the ExecutionCaps.total_buy_le_investable check compares THAT "
                             "— and only that — against plan.investable. _leg_plan_notional "
                             "FAILS CLOSED PER LEG: a symbol with no usable price in "
                             "`prices` falls back to that leg's own cap-priced notional, "
                             "which is strictly LARGER, so an unreadable price makes the "
                             "gate stricter, never looser. Price validity is judged by the "
                             "ONE shared rule (investable.usable_price, v0.42.0) — no second "
                             "validity rule was written. The reason string now reports BOTH "
                             "figures ('total BUY <plan> at plan prices > investable <inv> — "
                             "would over-deploy / use margin (worst case at the marketable "
                             "caps: <cap>)') and the [7] TOTALS preview line prints both, so "
                             "an operator sees exactly what the gate compares and what it "
                             "does not. "
                             "DELIBERATELY UNCHANGED, all still on the WORST-CASE total: "
                             "_buying_power_ok (whether the BROKER will permit the order "
                             "genuinely is a worst-case question), _margin_preflight_ok "
                             "(which ignores the figure on an unlevered plan anyway), and "
                             "ExecutionCaps.max_total_notional. "
                             "WHY THIS IS SAFE: the protection that ACTUALLY prevents "
                             "over-deployment is separate and untouched — the two-phase "
                             "transmit re-reads TotalCashValue FROM THE BROKER after the "
                             "sells settle and floors the buys to it via "
                             "_scale_buys_to_cash (CASH_SAFETY_BUFFER_PCT = 1%), guarded by "
                             "a hard assert. The plan-level check is an arithmetic "
                             "consistency test, not the cash rail. "
                             "TESTS: the U25274773 arithmetic is reproduced verbatim and "
                             "proven fail-before/pass-after by RESTORING HEAD's comparison "
                             "at runtime; a genuinely over-deployed plan (plan-basis total "
                             "above investable) still blocks and reports both figures; the "
                             "missing-price fallback is proven to FLIP a verdict; and "
                             "buying power is proven to still refuse on the worst case with "
                             "BuyingPower set between the two totals."),
    ("0.43.0", "2026-08-25", "THE AUTOMATED DAILY-LOSS HALT WAS REMOVED. OWNER DECISION "
                             "(Andrew, 2026-08-25). Order-affecting: it removes a condition "
                             "under which the desk refused to trade at all. This is a "
                             "DELETION, not a redesign — nothing replaces it, and it must "
                             "not be re-added. "
                             "WHY: it was NEVER AUTHORIZED. The -2%-of-NAV breaker "
                             "(config.RISK_LIMITS['max_daily_loss_pct_nav'] = 0.02) entered "
                             "in the PRE-GIT BASELINE SNAPSHOT 3b29616 and was never changed "
                             "and never decided; no decision record for it exists anywhere. "
                             "AND it is the WRONG CONSTRUCT for this desk. A "
                             "halt-until-cleared daily-loss breaker belongs to an intraday "
                             "trading book. This is a MONTHLY-REBALANCE ALLOCATION desk: the "
                             "breaker would refuse to rebalance the WHOLE BOOK on exactly the "
                             "down day you most want to rebalance (a rebalance BUYS what has "
                             "fallen), and — because trip_killswitch PERSISTED the trip to "
                             "local state — it would stay refused across restarts until a "
                             "human deleted a file by hand, silently benching every client "
                             "account in the meantime. A guard whose failure mode is 'nobody "
                             "gets rebalanced and nothing says so' is worse than no guard. "
                             "REMOVED: risk_manager._KILL_FILE, killswitch_state, "
                             "trip_killswitch, clear_killswitch and check_kill_switch, plus "
                             "the RISK_LIMITS entry that fed them. evaluate() no longer "
                             "consults daily_pnl for anything; halted/halt_reason are KEPT on "
                             "RiskReport (consumers read them) and are now ALWAYS False/'' "
                             "from this path. evaluate()'s SIGNATURE IS UNCHANGED — daily_pnl "
                             "is still accepted positionally for call-compatibility and is "
                             "still recorded in the audit ledger, but it gates nothing. "
                             "ALSO REMOVED, because it existed ONLY to feed that breaker: the "
                             "v0.41.0 strict-P&L wiring — execution_engine.read_daily_pnl and "
                             "DailyPnlUnavailable, and rebalance_execute's refusal to trade "
                             "an account whose daily P&L could not be read. An unreadable "
                             "P&L no longer benches an account; there is no longer anything "
                             "for it to blind. execution_engine._daily_pnl SURVIVES, "
                             "re-implemented standalone as a best-effort IBKR reqPnL read "
                             "returning 0.0 on any failure, because its only remaining "
                             "consumer is the ledger's AUDIT RECORD of the day's figure. "
                             "UNTOUCHED AND STILL LIVE — every other guard: the per-order "
                             "fat-finger rail (safe_execute.per_order_rail_reasons), the cash "
                             "reserve / no-leverage batch check, order "
                             "sanity, the margin pre-flight, the PDT gate, the corp-action "
                             "guard, the two-phase realized-cash gate, order_router's hard "
                             "price guard, the account wall and the arm/conform gates. "
                             "EXPLICITLY UNTOUCHED: the MANUAL, FILE-BASED operator stop — "
                             "the AUTOTRADE_DISABLED sentinel, the KILL_SWITCH label, "
                             "safe_execute.register_kill_switch_label and _kill_switch_present "
                             "on the live-deploy rails, and dashboard/desk/kill_switch.py. "
                             "That is a human deciding to stop the desk, which is a different "
                             "thing from a machine deciding, and it STAYS. TESTS: the tests "
                             "that asserted the breaker's wiring and the halt-on-unreadable-"
                             "P&L refusal are DELETED (they asserted behavior that is now "
                             "wrong); new paperbot/test_no_daily_loss_halt.py asserts the "
                             "ABSENCE of the mechanism — no check_kill_switch / "
                             "trip_killswitch / killswitch_state on risk_manager, no "
                             "max_daily_loss_pct_nav in RISK_LIMITS, and halted == False on a "
                             "-50%-of-NAV day. "
                             "ALSO IN THIS VERSION — THE 'GET FLAT' (FLATTEN) PANIC BUTTON WAS "
                             "REMOVED FROM THE DESK ENTIRELY. OWNER DECISION (Andrew, "
                             "2026-08-25), his reasoning: he does not want a panic button, "
                             "because he does not want to be making irrational decisions on bad "
                             "market days and he is not going to time the market. NOT "
                             "order-affecting — there was NEVER any order-transmit code behind "
                             "it: flatten_execute() was a hard stub that RAISED, and there was "
                             "no ib.placeOrder, no order router, no arm and no broker connection "
                             "anywhere in that module. It was a button that could only ever "
                             "print 'nothing to close'. DELETED: dashboard/desk/emergency."
                             "flatten_preview and flatten_execute, their "
                             "emergency_flatten_preview / emergency_flatten_blocked "
                             "plain-English event branches, and the render_emergency_bar() block "
                             "that drew the 'Type FLATTEN to confirm' input and the 'Get flat "
                             "(emergency close)' button; AND, IN THE SAME PASS — this is exactly "
                             "what stopped the removal the first time, and it is now done — "
                             "dashboard/desk/kill_switch.py's _do_flatten() and its three "
                             "--flatten-all / --flatten-s8 / --flatten-s0 CLI FLAGS, that "
                             "scaffold's only other caller. Those flags are now an argparse "
                             "error. HALT IS COMPLETELY UNTOUCHED: halt_strategy, halt_status, "
                             "the PowerShell builders, the three --halt-* flags and the whole "
                             "OS-level stop path are behaviourally byte-identical — stopping the "
                             "software is a control the owner keeps. NOT THIS, and deliberately "
                             "untouched: the MANUAL, file-based operator stop (the "
                             "AUTOTRADE_DISABLED sentinel / KILL_SWITCH label) — a different "
                             "control, and it STAYS. ALSO IN THIS VERSION, LATER THE SAME "
                             "SESSION — paperbot/flatten_accounts.py AND "
                             "paperbot/test_flatten_accounts.py WERE DELETED. OWNER DECISION "
                             "(Andrew, 2026-08-25): that tool was a scoped, paper-only, "
                             "dry-run-by-default cleanup utility (explicit --accounts and "
                             "--symbols allowlists) and it is NOT needed for live trading — "
                             "'just more shit to trip over'. Both files are gone, so it is no "
                             "longer the 'different tool that stays' described above. Two "
                             "COMMENT-ONLY references in paperbot/rebalance_execute.py were "
                             "rewritten to describe the TECHNIQUE (manually closing leftover / "
                             "off-model positions) instead of naming the deleted file, and "
                             "MONDAY_RUNBOOK.md's abort step now says to CLOSE POSITIONS BY "
                             "HAND IN THE GUI. No code, behaviour or test logic changed for "
                             "that deletion. "
                             "ALSO IN THIS VERSION — THE PER-POSITION CAP (35% OF NAV) WAS "
                             "REMOVED. OWNER DECISION (Andrew, 2026-08-25), reviewing the three "
                             "unauthorized invented numbers on this desk: only the drift band "
                             "survives. Order-affecting: it removes a condition under which a "
                             "single order was vetoed. WHY: it was NEVER AUTHORIZED — "
                             "config.RISK_LIMITS['max_position_pct_nav'] entered in the PRE-GIT "
                             "BASELINE SNAPSHOT 3b29616 with no decision record, exactly like "
                             "the daily-loss breaker — AND it had already been RETUNED ONCE to "
                             "fit the book it was supposed to police: its own comment admitted "
                             "the earlier 5% value 'would have vetoed the strategy itself', "
                             "because Adaptive All-Weather legitimately holds ~26.67% in each "
                             "equity-core ETF. A threshold moved until it stops objecting is not "
                             "a control. The strategy's own per-asset caps (SPEC §12) are the "
                             "real per-position constraint. REMOVED: the RISK_LIMITS entry, "
                             "risk_manager's per-order cap check, and the unconditional "
                             "`resulting` share book that existed only to feed it. KEPT: "
                             "risk_manager.CASH_EQUIVALENTS and its strat_config import — the "
                             "cash-reserve / no-leverage BATCH guard still reads them to decide "
                             "what counts toward the liquid reserve, so they are NOT dead code. "
                             "The unpriced-price order-level refusal is UNCHANGED IN BEHAVIOUR; "
                             "only its wording changed, because it named a cap that no longer "
                             "exists. REVIEWED IN THE SAME PASS AND DELIBERATELY KEPT: "
                             "config.ORDER_CAP_K = 0.003 (the 30bps marketable cap — a "
                             "worst-case execution price on a ladder rung, not a position "
                             "limit) and config.REBALANCE_BAND_PCT = 0.03 (the 3% no-trade drift "
                             "band). Both stay by explicit owner decision. Also untouched: the "
                             "single-order-notional sanity guard (one order may not exceed NAV — "
                             "still live, still tested), the cash reserve, the max-legs guard "
                             "and all v0.42.0 price logic. TESTS FOR BOTH REMOVALS: "
                             "paperbot/test_no_daily_loss_halt.py gains cases asserting "
                             "max_position_pct_nav is absent from RISK_LIMITS and that an order "
                             "leaving a single risk position at ~90% of NAV is APPROVED while an "
                             "order whose notional exceeds NAV is STILL vetoed; new "
                             "dashboard/desk/test_no_flatten.py asserts emergency has no "
                             "flatten_preview / flatten_execute attribute, that "
                             "kill_switch.main(['--flatten-all']) now raises SystemExit as an "
                             "argparse error, and that --halt-all still dispatches to the "
                             "unchanged halt path. ONE PRE-EXISTING ASSERTION WAS UPDATED, not "
                             "deleted: test_unpriced_not_zero.py's "
                             "test_oversized_order_with_good_prices_is_still_vetoed_the_same_way "
                             "required BOTH 'exceeds NAV' AND 'per-position cap' in the veto "
                             "reasons; it now asserts the surviving NAV guard still fires and "
                             "that no cap message appears."),
    ("0.42.0", "2026-08-25", "A MISSING OR NaN PRICE WAS TREATED AS THE NUMBER ZERO, AND "
                             "ZERO MEANS SOMETHING VERY DIFFERENT. Order-affecting: it "
                             "changes what gets sized, what gets sold, and whether a guard "
                             "fires at all. ONE root cause, three consequences. "
                             "(1) SIZING. reconcile sized a symbol it could not price as "
                             "target_shares = 0 — the identical value 'the model wants none "
                             "of this' produces. rebalance_engine then emitted "
                             "delta = 0 - held, so a 50%-weight model holding we merely "
                             "failed to quote was sized as a FULL LIQUIDATION (proven: "
                             "plan.orders = {'AAA': -100}). The commoner mirror was silent: "
                             "an unquoted model symbol the account does not hold contributed "
                             "a 0-share delta and 0.0 trade weight, so the account was "
                             "reported 'In-spec — already conform, nothing to trade' while "
                             "holding none of that sleeve. Now: status UNPRICED, target "
                             "pinned to shares held (delta exactly 0, never a sell), and the "
                             "account reports NOT in spec. HELD + unpriced BLOCKS the "
                             "account (its value is inside NetLiq but unaccounted for, so "
                             "the siblings cannot be sized) via the SAME blocked_reasons "
                             "road the held-aside carve-out already uses; NOT held is "
                             "ISOLATED — that sleeve stays in cash and the rest of the "
                             "account still rebalances, so one bad quote never benches a "
                             "whole book. weight == 0 WITH a good quote still SELLS: "
                             "legitimate rotation, deliberately unchanged. "
                             "(2) GUARDS. risk_manager.price_of turned a missing price into "
                             "0.0 and passed a NaN straight into comparisons that are then "
                             "ALL False. Either one disabled EVERY price-based guard for the "
                             "account: a plainly levered book read as fully reserved "
                             "(batch_reasons went from ['liquid reserve -20.00% < required "
                             "1.50%'] to []), and an oversized order vetoed twice with good "
                             "prices was APPROVED with one NaN price. A guard that cannot "
                             "see must REFUSE, not report clear: it now fails closed with a "
                             "plain-English reason naming the symbol — per-ORDER where the "
                             "gap is isolated to one leg, per-BATCH for the leverage check, "
                             "which sums every holding and genuinely cannot be isolated. "
                             "(3) PRICING SOURCE (owner decision). The desk does not "
                             "maintain prices for execution — IBKR is the price source. "
                             "Every execution rail used to fall back to the model's stored "
                             "daily CLOSE when a quote was missing, and the batch rail then "
                             "dropped the key with no tally, no counter and no warning. A "
                             "stale close is not a price you can trade at. Removed from "
                             "batch_rebalance_execute, s0_live_deploy, s0_live_exec, "
                             "s0_live_pilot_run, live_fa_block_execute, rebalance_execute, "
                             "rebalance_run, nightly_monitor_run, rehearse_dedup_gate_stage "
                             "and execution_engine.compute_intended_orders, which also "
                             "stopped emitting a 0.0 limit price. One helper "
                             "(live_quotes.execution_prices) now makes that decision and "
                             "COUNTS AND NAMES every symbol IBKR would not quote. Sizing "
                             "opts in via strict_prices=True; the backtester and offline "
                             "readouts still use the strategy's own price HISTORY, which is "
                             "what it is for — that is not execution and is untouched. "
                             "investable.usable_price is the single price gate: None, NaN, "
                             "non-numeric and non-positive all mean UNPRICED, never 0.0."),
    ("0.41.0", "2026-08-25", "THREE EXISTING SAFETY GUARDS WERE NOT WIRED INTO SOME "
                             "ORDER-GENERATING RAILS. Order-affecting: each changes whether "
                             "a rail trades at all, or what it sizes. No guard was "
                             "redesigned — each was already written and tested and simply "
                             "was not connected. "
                             "(1) THE DAILY-LOSS KILL SWITCH WAS FED A HARDCODED ZERO. "
                             "risk_manager.check_kill_switch computes "
                             "loss_pct = daily_pnl / nav and halts at max_daily_loss_pct_nav "
                             "(-2%). rebalance_execute — the transmit-capable multi-account "
                             "rail — passed 0.0, so loss_pct was ALWAYS 0.0: the breaker was "
                             "structurally inert, every account printed 'clear' and every "
                             "ledger record said halted:false however bad the day was. It now "
                             "passes each account's REAL daily P&L, read through the desk's "
                             "ONE reader (execution_engine, IBKR reqPnL) rather than a second "
                             "implementation. That reader is now split: read_daily_pnl RAISES "
                             "DailyPnlUnavailable, while the pre-existing best-effort "
                             "_daily_pnl keeps its 0.0-on-failure behavior for the per-account "
                             "engine, unchanged. The rebalance rail deliberately does NOT "
                             "inherit that soft zero — an unreadable P&L is indistinguishable "
                             "from a genuinely flat day — so an account whose P&L cannot be "
                             "read is HALTED with a plain-English reason and risk_manager is "
                             "never consulted for it. A guard that cannot see must not report "
                             "'clear'. "
                             "(2) THE CORP-ACTION GUARD WAS OFF ON TWO RAILS. "
                             "reconcile.classify_untracked needs the strategy's tradeable "
                             "universe to split a held symbol into ALIEN / FRACTIONAL / SWEEP "
                             "(no auto-trade, parked for human review) versus ROTATE_OUT (a "
                             "symbol the model genuinely dropped, sell). With universe=None "
                             "every one of them collapses to UNTRACKED, an ALWAYS-BREACH "
                             "status that sizes as delta = 0 - held: a FULL LIQUIDATION of a "
                             "spinoff, a rename, a client's own holding or a money-market "
                             "sweep. rebalance_execute (via rebalance_run.build_preview, which "
                             "did not even accept the parameter) and live_fa_block_execute "
                             "both passed none; both now resolve it and FAIL CLOSED if they "
                             "cannot. Related: s0_live_pilot_run._strategy_universe swallows "
                             "every exception and returns None, justified as safe because ITS "
                             "preview is zero-transmit — but "
                             "batch_rebalance_execute.run_batch_session imports and calls that "
                             "same function, so one failed import silently disarmed the guard "
                             "on an armed multi-account rail. That rail (and s0_live_deploy / "
                             "s0_live_exec) now REFUSE on a missing universe instead of "
                             "proceeding; the batch refusal fires before the broker is read "
                             "and applies in PREVIEW too, because a preview whose "
                             "classification is wrong is exactly what a human then arms. The "
                             "zero-transmit pilot keeps its soft fallback. "
                             "(3) THE BOND CARVE-OUT WAS NOT WIRED INTO THE REAL-MONEY "
                             "SINGLE-ACCOUNT DEPLOY. s0_live_deploy and s0_live_exec called "
                             "rebalance_engine.plan_account with universe= but no sec_types= "
                             "and no values=, so holding_class.carve_out short-circuited to "
                             "'nothing held aside' and an individual bond was sized INSIDE the "
                             "target allocation — contrary to owner decision D6, which puts "
                             "held-aside holdings OUTSIDE it and applies the model to the "
                             "remaining sleeve as its own 100%. Both now pass the broker's OWN "
                             "contract.secType (never a guess off the ticker text) plus "
                             "recon_report._portfolio_values as the held-aside pricing "
                             "fallback, mirroring batch_rebalance_execute and "
                             "live_fa_block_execute. An unpriceable held-aside holding still "
                             "withholds orders (fail closed), never a silent zero. "
                             "s0_live_deploy is the rail that has actually traded real money "
                             "on the funded trust account U14438624."),
    ("0.40.0", "2026-08-25", "CUSTOM SIZE-TIER: MULTI-RUNG MOVES GO DIRECTLY TO TARGET, AND "
                             "THE TWO CRM HISTORY INPUTS ARE NOW READ. Order-affecting: it "
                             "changes which model an account trades and how many times it "
                             "trades to get there. Completes the 0.39.0 rule, which was "
                             "faithful to an under-specified spec in two places that both "
                             "diverged from the CRM's SQL half. "
                             "(1) DIRECT TO TARGET. The rule is now stated as CURRENT rung vs "
                             "NATURAL rung (natural = the PLAIN boundaries: < 5,000 starter, "
                             "5,000-25,000 small, >= 25,000 full). natural == current -> STAY. "
                             "ADJACENT -> that boundary's band decides "
                             "(starter->small >= 5,500; small->starter < 4,500; "
                             "small->full >= 27,500; full->small < 22,500), else STAY. TWO "
                             "RUNGS APART (starter<->full) -> MOVE DIRECTLY, NO BAND. WHY: "
                             "0.39.0 walked one rung per run, so a Starter account funded to "
                             "$30,000 traded into the 11-line small book tonight and the "
                             "15-line full book tomorrow — the spread paid TWICE for an "
                             "intermediate book nobody chose. A band exists to damp "
                             "oscillation around a boundary, and $30,000 is nowhere near the "
                             "$5,000 boundary, so there is nothing to damp. Risk level is "
                             "still PRESERVED from the incumbent for small<->full and "
                             "RECOVERED from history whenever LEAVING starter — now including "
                             "the two-rung starter->full jump — defaulting to GROWTH. "
                             "(2) THE TWO CRM INPUTS. v_tradingdesk_roster now publishes "
                             "has_prior_custom_assignment (boolean NOT NULL) and "
                             "prior_custom_risk_level (text NULL), computed CRM-side by the "
                             "same job that runs the SQL ladder. BOTH are added to the "
                             "EXPLICIT SELECT list in crm_roster.fetch_roster — a view-only "
                             "change would never reach the desk — and passed into custom_tier "
                             "as PARAMETERS, so that module stays pure and the desk still "
                             "reads the CRM strictly READ-ONLY (nothing is written back). "
                             "has_prior_custom_assignment replaces 0.39.0's "
                             "'current_label is None' test for a FIRST assignment, which was "
                             "the real divergence: a freshly assigned $4,900 account resolved "
                             "to 'Starter (Custom)' in the CRM but to 'Growth (Small, Custom)' "
                             "on the desk, because the desk saw a label and treated a "
                             "brand-new assignment as a sticky incumbent. Verified by running "
                             "both. Both columns are read DEFENSIVELY (a missing key or NULL "
                             "cannot raise) so a lagging view degrades instead of breaking, "
                             "and a MISSING has_prior flag fails toward treating the account "
                             "as an INCUMBENT — applying the band — because silently "
                             "re-tiering a live account off a plain boundary is the more "
                             "damaging error. DEPLOY ORDER: the view must carry both columns "
                             "before this ships, or every roster read raises "
                             "CrmRosterUnavailable and the desk falls back to "
                             "config.ENROLLMENT."),
    ("0.39.0", "2026-08-25", "CUSTOM-FAMILY SIZE-TIER CHECK AT THE MOMENT OF USE. "
                             "Order-affecting: it changes WHICH MODEL an account trades. New "
                             "PURE module paperbot/custom_tier.py, wired into "
                             "batch_rebalance_execute.resolve_roster_versions where a custom "
                             "label previously skipped the pre-trade NAV re-check entirely. "
                             "LADDER (3 whole-share rungs): full = 15-line '<Risk> (Custom)', "
                             "small = 11-line '<Risk> (Small, Custom)', starter = 2-line "
                             "'Starter (Custom)' (SCHB 80 / USFR 20). The starter rung is "
                             "MEASURED, not preference: at $2,000 the 11-line small book "
                             "deploys only 76.2% — its 3% long-Treasury and gold slices are "
                             "worth less than one share so those orders are never created — "
                             "while the 2-line book deploys 95.9%. HYSTERESIS, two bands; the "
                             "target depends on the CURRENT rung, not the value alone: "
                             "starter->small at >= $5,500; small->starter below $4,500; "
                             "small->full at >= $27,500; full->small below $22,500; else stay. "
                             "$22,500/$27,500 is the existing house band around $25,000 "
                             "(small_tier._bounds and the CRM's run_model_tier_scan both use "
                             "it); $4,500/$5,500 is the same +/-10% shape around $5,000. A "
                             "FIRST assignment (no current custom model) has no incumbent to "
                             "be sticky about and uses the PLAIN boundaries $5,000 / $25,000 — "
                             "existing house behaviour. One rung per evaluation: those four "
                             "transitions are the whole state machine. RISK LEVEL: "
                             "small<->full preserves it (both labels carry it); "
                             "'Starter (Custom)' is ONE book shared by all three risk levels "
                             "and carries none, so starter->small must RECOVER it from the "
                             "CRM's assignment history, and where no prior non-Starter custom "
                             "model exists it defaults to GROWTH (Andrew 2026-08-25: a small "
                             "balance moving up from $5,000 has no reason to be less than "
                             "growth) — a decision, never a flag or a skip. That history is "
                             "NOT readable today (v_tradingdesk_roster exposes no assignment "
                             "history; see custom_tier.PRIOR_RISK_FIELD), so every promotion "
                             "currently takes the GROWTH default. The desk reads the CRM "
                             "READ-ONLY: no local store was invented and nothing is written "
                             "back. WHY small_tier IS DELIBERATELY NOT REUSED: its "
                             "parent_version / is_small / tier_for are SPELLING-based, so "
                             "handing them a hand-authored label is one CRM rename away from "
                             "rewriting the account onto an S0 model and silently discarding "
                             "the whole book. custom_tier imports small_tier NOWHERE and "
                             "dispatches on a CLOSED enumerated label set; every label it can "
                             "emit comes out of that same table, so a custom book can never "
                             "become an S0 model, and a label outside the family (an S0 label, "
                             "or a renamed custom one) is refused and passed through "
                             "UNCHANGED. The CRM runs the identical ladder in SQL — the two "
                             "must agree exactly or accounts oscillate between models daily."),
    ("0.38.0", "2026-08-25", "PER-MODEL CASH RESERVE (CUSTOM ALLOCATIONS 1%, S0 STAYS 1.5%) "
                             "+ THE PRICE UNIVERSE THOSE ALLOCATIONS NEED. Order-affecting: "
                             "it changes how much of each account is deployed. "
                             "(1) PRICE UNIVERSE. strategies/config.py gains "
                             "CUSTOM_ALLOCATION_TICKERS (JAAA, BUCK, SGDM, GDXJ, SILJ) and "
                             "backtester/src/download_data.py unions it into the DOWNLOAD set "
                             "only. S0's ALL_TICKERS is untouched, so no S0 model, universe or "
                             "parameter moves. Why it is needed: custom_target loads prices "
                             "for the allocation's EXPLICIT ticker list and raises "
                             "CustomAllocationError when one has no history, so a published "
                             "allocation naming an unstocked ticker fails the whole account's "
                             "target build (fail-closed) rather than trading a partial book. "
                             "(2) PER-MODEL RESERVE. cash_reserve_pct was ONE global knob "
                             "(0.015) read by every consumer through investable.buffer_pct(). "
                             "It is now resolved PER MODEL: config gains "
                             "custom_allocation_cash_reserve_pct=0.01 and "
                             "investable.buffer_pct_for(is_custom) picks between them. WHY 1%: "
                             "the reserve is operational, not cosmetic — IBKR deducts its "
                             "advisory fee from account CASH and client distributions are paid "
                             "from cash, so a fully-invested account is overdrawn the moment a "
                             "fee posts (the 2026-07-28 negative-balance incident). 1% is "
                             "Andrew's call: enough fee/distribution headroom while leaving "
                             "less of the client's money undeployed. WHY S0 STAYS AT 1.5%: it "
                             "is a validated model and nothing about it is being re-validated "
                             "here; anything that does not name its own reserve keeps the "
                             "global, so every pre-existing caller is byte-identical. "
                             "(3) THE VALUE REACHES EVERY CONSUMER, NOT JUST SIZING — the "
                             "correctness risk. The reserve is read at a SIZING site "
                             "(investable = (NAV-reserve)*(1-pct)) and at a MEASUREMENT site "
                             "(the synthetic CASH bucket's target weight). Size at 1% while "
                             "measuring at 1.5% and the account holds exactly what it was told "
                             "to and still reads 0.5% adrift on cash forever: it can never "
                             "reconcile, every readout calls it out-of-spec, and anything "
                             "acting on drift churns it. So ONE value per account is threaded "
                             "from the caller through rebalance_engine.plan_account into BOTH "
                             "compute_investable and reconcile.reconcile (which now applies it "
                             "to the CASH line and to its default investable), and the same "
                             "override was added to plan_accounts/build_plan (per-version map), "
                             "recon_report.plan_account, execution_engine."
                             "compute_intended_orders, risk_manager.evaluate (a book sized to "
                             "1% must be reserve-checked at 1%, or the no-leverage guard vetoes "
                             "it), crm_outofspec.scan_out_of_spec and the dashboard's "
                             "custom-allocation drift scan. AccountPlan now records the reserve "
                             "that sized it. "
                             "(4) DISPATCH IS SOURCE-BASED, like the rest of the custom "
                             "feature: the executor reads the reserve off the AllocationMeta it "
                             "already has (a label with rows in v_tradingdesk_custom_"
                             "allocations), never off the label's spelling, so a CRM rename "
                             "cannot move an account's reserve in either direction. A label "
                             "absent from the map gets the global — a missing CRM read degrades "
                             "to today's behavior, never to a 0% reserve."),
    ("0.37.0", "2026-08-25", "ANDREW-AUTHORED (CUSTOM) MODEL PORTFOLIOS WIRED INTO THE BATCH "
                             "REBALANCE EXECUTOR + THAT EXECUTOR'S MISSING AUDIT TRAIL. Both "
                             "in paperbot/batch_rebalance_execute.py (plus a reader added to "
                             "paperbot/ledger.py); both change what gets ordered and what is "
                             "recorded about it, so one version covers both. "
                             "(1) TARGET DISPATCH. Andrew authors tickers+percentages in the "
                             "CRM; the desk now reads them READ-ONLY and rebalances to them "
                             "exactly like an S0 model. Dispatch is by the allocation's SOURCE "
                             "(does the label have rows in v_tradingdesk_custom_allocations?), "
                             "never by the label's spelling — a CRM rename must not be able to "
                             "route a hand-authored book into the S0 backtester. Custom targets "
                             "are built by custom_target.custom_targets_with_meta; S0 targets "
                             "are built exactly as before. The pre-existing fail-closed "
                             "contract is preserved verbatim: a target that cannot be built "
                             "prints COULD NOT BUILD TARGET and returns 2, having connected to "
                             "nothing — a custom target fails just as loudly, because the "
                             "alternatives (falling through to the backtester, or an empty "
                             "Target) would size an account against a book Andrew never wrote, "
                             "and an empty Target LIQUIDATES the account. "
                             "(2) SMALL-TIER NAV OVERRIDE EXCLUDED FOR CUSTOM LABELS, "
                             "EXPLICITLY. resolve_roster_versions re-tiers an account by NAV "
                             "via small_tier.parent_version/tier_for, which are SPELLING-based "
                             "and can REWRITE the account's label. It was safe only because "
                             "'Growth (Small, Custom)' happens not to end in ' (Small)'. That "
                             "is a coincidence of naming, not a control: one CRM rename and the "
                             "override silently re-points the account onto an S0 model, "
                             "discarding the whole allocation. The exclusion is now source-"
                             "based and tested with the SAME label + NAV on both sides. "
                             "(3) THE ALIEN/UNIVERSE TRAP — the order-affecting one. Every "
                             "account was sized with universe=S0's ALL_TICKERS. reconcile "
                             "classifies a held, weight-0 symbol OUTSIDE the universe as ALIEN, "
                             "and ALIEN is in rebalance_engine._NO_AUTOTRADE_STATUSES: it never "
                             "breaches the band and never produces a delta. So a ticker Andrew "
                             "REMOVED from his allocation would sit in the account forever, "
                             "never sold, with no error anywhere — the rotation silently doing "
                             "nothing. A custom-allocation account is now sized against a "
                             "PER-ACCOUNT universe (custom_target.universe_for: the "
                             "allocation's tickers plus that account's held symbols), so a "
                             "removed ticker classifies ROTATE_OUT and is SOLD. Stated "
                             "trade-off: those held symbols lose ALIEN human-review status; the "
                             "sizing loop names them on every run. S0 accounts are unchanged. "
                             "(4) THE AUDIT GAP. This executor — the one the Control Plane "
                             "shells out to — wrote NO ledger record at all, so a batch run "
                             "left only the per-leg transmit_journal and the orderRef: no "
                             "stored target weights, no model label. Worse, the ref encodes "
                             "target.as_of, which for a custom allocation is a DATE, so two "
                             "allocations published the same day are INDISTINGUISHABLE on the "
                             "wire. Every batch run (PREVIEW and ARMED, S0 and custom alike) "
                             "now writes ONE ledger.record_run modelled on "
                             "execution_engine._run_record, carrying run_id and, per account, "
                             "the model label, target_as_of, target_weights, legs, orderRefs, "
                             "outcome — and, where the model is a custom allocation, the "
                             "published version_number/version_id. One batch run_id is stamped "
                             "onto every ExecutionRequest so all of the run's refs share it, "
                             "and ledger gains a minimal reader (iter_runs/find_run) so the "
                             "join orderRef -> run_id -> record -> allocation version is "
                             "actually traversable. Nothing about arming, the account wall, the "
                             "kill switch or the two-phase transmit changed."),
    ("0.36.0", "2026-08-25", "FA-BLOCK RAIL: HELD-ASIDE CARVE-OUT WIRED IN + A PATTERN-DAY-"
                             "TRADER GATE ADDED. Both live in paperbot/live_fa_block_execute.py "
                             "— the rail owner decision D1 chose for the WHOLE book — and both "
                             "change what gets ordered, so one version covers both. "
                             "(1) HELD-ASIDE CARVE-OUT (owner decision D6). PROBLEM: this lane "
                             "built its engine inputs with only account/version/net_liq/"
                             "positions/prices. rebalance_engine.plan_account therefore read "
                             "sec_types=None and holding_class.carve_out short-circuited to "
                             "'nothing held aside', so a bond-holding account had its model "
                             "weights sized against its FULL NetLiq INCLUDING the bond — the "
                             "bond silently INSIDE the target allocation, when D6 puts "
                             "held-aside holdings OUTSIDE it and applies the model to the "
                             "remaining managed sleeve as its own 100%. The per-account batch "
                             "rail (batch_rebalance_execute.py) has always passed sec_types; "
                             "only the block rail did not. FIX: the broker read is factored "
                             "into build_account_inputs(), which keeps the RAW position objects "
                             "and builds sec_types from each contract.secType (same "
                             "construction as the batch rail) plus values from "
                             "recon_report._portfolio_values (the pricing fallback for a bond "
                             "with no strategy close and no quote; failure degrades to {} and "
                             "the plan withholds orders rather than guessing a zero). _plan_for "
                             "now threads both through as well, so the margin pre-flight's "
                             "re-derived plan is byte-identical to the routed one instead of "
                             "re-planning the account against the full NetLiq. "
                             "(2) PATTERN-DAY-TRADER GATE — NEW; no PDT gate existed on ANY "
                             "lane. EVIDENCE: on 2026-07-28 account U5721712 rejected a plain "
                             "BUY of 1 USFR (~$50) with NO offsetting sell — an account IBKR "
                             "has ALREADY flagged PDT under $25k equity rejects ORDINARY "
                             "orders regardless of order shape, so an order-shape 'does this "
                             "create a day trade' analyzer is the wrong instrument. SCALE: 113 "
                             "of 304 roster accounts are margin under $25,000 (53 in Andrew's "
                             "book), none flagged no_trade; U5721712 is still on the roster at "
                             "$957.10 with no_trade=false. Today's only mitigation is that S0 "
                             "trades ONE hand-picked PDT-clear account, which evaporates going "
                             "book-wide under D1. FIX: pdt_preflight_over_split(), a pure "
                             "sibling of margin_preflight_over_split, reads IBKR's "
                             "DayTradesRemaining out of the per-account accountSummary rows "
                             "the lane ALREADY holds — ZERO new broker reads (ib_async requests "
                             "the tag by default). Semantics: -1 unlimited and any positive "
                             "count CLEAR; 0 REFUSES; missing/unparseable FAILS CLOSED (this "
                             "lane's stance, not _buying_power_ok's fail-open). VERIFIED "
                             "READ-ONLY against the live-trade gateway (port 4003) 2026-08-25 "
                             "before building: the tag comes back PER SUB-ACCOUNT — U14438624 "
                             "'-1', U5721712 '0' — and NOT on the aggregate 'All' scope. The "
                             "gate runs in _execute_one_route immediately after the margin "
                             "pre-flight and BEFORE the group-XML diff and "
                             "set_group_contracts_or_shares, so a refused account can never "
                             "cause a replaceFA write. A blocked account is DROPPED from "
                             "per_account_split (dc_replace + recomputed total_qty, exactly as "
                             "the realized-cash re-sizer does) and the block is refused "
                             "(SKIPPED_PDT) ONLY if the split empties — one $957 account must "
                             "not veto a 60-account rebalance. Every drop is LOUD: a "
                             "PATTERN-DAY-TRADER EXCEPTIONS section in the run report (preview "
                             "AND armed) and top-level pdt_dropped / pdt_refused_blocks in the "
                             "audit ledger record. A silently un-rebalanced account is the "
                             "failure mode that let an $826k rollover sit idle 131 days."),
    ("0.35.0", "2026-08-25", "PER-ORDER FAT-FINGER RAIL REBUILT — the flat 50%-of-NetLiq cap "
                             "REMOVED (owner decision D3, 2026-08-19: 'NO ORDER CAPS. %NLV and "
                             "max-notional caps just create issues'). PROBLEM: "
                             "safe_execute.MAX_ORDER_NOTIONAL_PCT_NLV = 0.50 was the default "
                             "for ExecutionCaps.per_order_notional_pct_nlv and was passed "
                             "explicitly by s0_live_deploy, so no single order could exceed 50% "
                             "of NetLiq. That is STRUCTURALLY wrong for a first deployment: a "
                             "never-invested account buys the model's dominant leg at its full "
                             "model weight, which is ~85% of NetLiq BY CONSTRUCTION (Growth "
                             "(Small) = SCHB 85% + USFR 15%), so the cap blocked the INITIAL "
                             "DEPLOY OF EVERY NEVER-INVESTED ACCOUNT even when fully armed. "
                             "EVIDENCE: the 2026-08-25 read-only whole-roster preview on "
                             "U5721712 (Growth (Small), NetLiq 957.10, zero positions) — 'order "
                             "BUY SCHB x27 notional 800.55 > 50% of NetLiq (478.55)'. The "
                             "roster deliberately carries never-invested cash accounts since "
                             "the 2026-08-24 funded-reality change, and the CRM separately "
                             "flags $1.48M across 9 never-invested accounts including an $826k "
                             "rollover idle 131 days. FIX (not a deletion — the rail is "
                             "re-based on the PLAN instead of an arbitrary slice of NetLiq): "
                             "ExecutionCaps.per_order_notional_pct_nlv -> "
                             "per_order_model_multiple (default MAX_ORDER_MODEL_MULTIPLE = "
                             "2.0), enforced by the new PURE safe_execute.per_order_rail_"
                             "reasons(). A BUY leg's notional may not exceed 2x the model's OWN "
                             "target dollars for that symbol (target weight x MANAGED NetLiq — "
                             "the held-aside-carved sleeve the weights actually apply to), "
                             "floored at one share's limit so a single share is never refused. "
                             "A SELL leg's QUANTITY may not exceed the shares the account "
                             "actually holds, read off the plan's own lines/alien_lines (this "
                             "desk never shorts, and by construction |delta| <= "
                             "int(actual_shares), an alien liquidation being exactly that) — an "
                             "exact invariant with zero false blocks; a plan carrying no "
                             "holding record for the symbol falls back to notional <= 2x "
                             "managed NetLiq as a pure implausibility backstop. NET SAFETY: "
                             "STRICTLY TIGHTER on both sides that can actually hurt. A 10x "
                             "quantity or a 10x limit price on a BUY still blows through the "
                             "multiple; an oversized SELL — which the %NLV cap waved through "
                             "whenever the position sat under half of NetLiq — is now refused "
                             "outright. A too-high SELL limit was never the risk (a limit order "
                             "does not fill through it), which is why the sell rail is on "
                             "shares, not notional. UNTOUCHED: total_buy_le_investable (an "
                             "arithmetic no-over-deploy check, not a cap in D3's sense; it "
                             "already bounds every BUY leg by investable capital), "
                             "max_total_notional (still None/off by default), the two-phase "
                             "realized-cash gate, order_router's HARD price guard, the account "
                             "wall, the arm/conform/kill gates and the margin pre-flight. "
                             "SCOPE: applies to every execute_plan caller — s0_live_deploy "
                             "(DEPLOY), crm_execute/batch_rebalance_execute (REBALANCE). "
                             "live_fa_block_execute does NOT go through execute_plan and never "
                             "carried this cap; its own cash-gate invariant is unchanged. "
                             "PURPOSE-BLIND ON PURPOSE: the rail is correct for DEPLOY and "
                             "REBALANCE alike, so no PURPOSE_DEPLOY exemption was added — an "
                             "exemption would have left the ongoing lane still false-blocking a "
                             "legitimate large sell-down. TESTS: 6 tests fail before / pass "
                             "after (never-invested first deploy permitted at unit and "
                             "end-to-end level; oversized BUY refused; oversized SELL refused; "
                             "ongoing rebalance sell-down + top-up permitted)."),
    ("0.34.0", "2026-08-19", "PER-RUN ORDER-REF STAMP on the FA-BLOCK lane and the shared "
                             "paper REBALANCE lane (owner decision 2026-08-19). PROBLEM: the "
                             "orderRef was paperbot:{account|fa_group}:{as_of}:{side}:{symbol} "
                             "and `as_of` is the MODEL as-of — for a monthly model, effectively "
                             "a MONTH stamp. order_router.place dedups on that ref, so a SECOND "
                             "run of the same group+symbol+side inside the month was classified "
                             "'an identical WORKING order already exists' and silently sent "
                             "NOTHING. That is the 2026-07-28 root cause (a bought-then-sold "
                             "symbol back below target looked 'already done' and could not be "
                             "re-bought); it was fixed then on the PER-ACCOUNT lane only "
                             "(safe_execute._deploy_ref/_run_id) and the BLOCK lane was never "
                             "moved over. FIX: order_router._run_stamp appends a per-run "
                             "wall-clock id to the ref (new ref: ...:{side}:{symbol}:{run_id}); "
                             "_order_ref / the new _fa_block_ref / build / build_fa_block / "
                             "order_ref_for_route all take run_id. live_fa_block_execute mints "
                             "ONE run_id per call to execute_fa_block_routes (both cash-gate "
                             "phases share it) and rebalance_execute mints ONE per armed "
                             "session, both via safe_execute._run_id — one convention desk-wide, "
                             "not a second format. PRESERVED: within a run the stamp is "
                             "constant, so already_present still blocks a retry / straggler "
                             "re-price / crash-resume from double-submitting a leg, and `side` "
                             "in the ref keeps a SELL and a BUY of the same symbol in the same "
                             "run apart. DELIBERATELY DROPPED: cross-run suppression — a NEW run "
                             "is now correctly NEW WORK, with the engine's delta-vs-positions as "
                             "the sole source of truth. AUDIT: run_id is a readable "
                             "YYYYmmddTHHMMSS stamp AND is written to both lanes' ledger "
                             "records, so an orderRef on the wire joins back to its run. "
                             "UNCHANGED ON PURPOSE: morning_execute_run keeps the base (no "
                             "run_id) ref — its dedup keys a DURABLE transmit journal on the "
                             "ref, where cross-run 'already sent today' is the intended "
                             "behavior and a per-run ref would silently disable it."),
    ("0.33.0", "2026-08-19", "TWO order-affecting change-sets. "
                             "(1) BOND / HELD-ASIDE POSITION CARVE-OUT (owner decision "
                             "2026-08-19). PROBLEM: v0.30.0 valued an individual bond "
                             "correctly but BENCHED the whole account for holding one "
                             "('manual liquidation required (bond)'), so a client with a "
                             "single bond never got their tradeable sleeve rebalanced — and a "
                             "bond that reached the engine as an ALIEN line could in principle "
                             "have been LIQUIDATED by a conform run. DECISION: treat them like "
                             "a NO-TRADE LIST, account for them like professionals, and put "
                             "them OUTSIDE the target allocation. FIX: new leaf module "
                             "paperbot/holding_class.py (stdlib only — no import cycle) is the "
                             "ONE place that classifies a holding MANAGED vs HELD_ASIDE, "
                             "strictly off the INSTRUMENT TYPE (contract.secType on the live/"
                             "IBKR lane, asset_category on the CRM lane) — NEVER a symbol "
                             "string. carve_out() values the held-aside block (per-type "
                             "PRICE_MULTIPLIER: a BOND is qty*mark/100, face qty x percent-of-"
                             "par, matching IBKR's own market_value) and removes it from NetLiq "
                             "FIRST; rebalance_engine.plan_account then composes the two "
                             "carve-outs in order — managed_net_liq = net_liq - "
                             "held_aside_value, investable = (managed_net_liq - reserve)*"
                             "(1-cash_reserve_pct) — reconciles ONLY managed positions against "
                             "managed_net_liq (the model is a complete portfolio for the "
                             "sleeve, as its own 100%), and measures the band on that same "
                             "sleeve (a 4% drift in the tradeable half of a half-bond account "
                             "is a real 4% breach, not a diluted 2%). The distribution reserve "
                             "is still computed on the WHOLE account (a client's distribution "
                             "obligation does not shrink because part of their money sits in "
                             "bonds) but is raised from the sleeve, the only place cash can "
                             "come from. Held-aside holdings never reach reconcile at all, so "
                             "they can never become a drift gap, an UNTRACKED/ALIEN line, or a "
                             "leg of ANY kind — not a buy, not a sell, not an ALIEN "
                             "liquidation. They stay PRICED, COUNTED and NAMED: AccountPlan "
                             "gains managed_net_liq/held_aside_value/held_aside/"
                             "blocked_reasons, surfaced through recon_report, crm_outofspec, "
                             "batch_rebalance_execute, the daily out-of-spec check and the "
                             "Action Center / Control Plane pages (a bond is reported, NOT a "
                             "defect and NOT a pending manual sale). TWO FAIL-CLOSED edges: an "
                             "instrument type that is blank/absent/unrecognised is HELD ASIDE "
                             "and flagged needs_classification (never silently assumed "
                             "tradeable), and a held-aside holding that cannot be PRICED BLOCKS "
                             "that account's orders with a named reason (orders={}, "
                             "breached=False) rather than under-carving NetLiq and sizing the "
                             "sleeve off a bad base. Extensible by class, not by special case: "
                             "a new no-trade instrument type is a one-line HELD_ASIDE_TYPES "
                             "edit because every consumer keys off the CLASS, never off 'is it "
                             "a bond'. Order-affecting (changes the sizing denominator and "
                             "which holdings can produce a leg) but sec_types=None — the "
                             "default and every pre-existing caller, incl. the backtester — "
                             "carves out NOTHING and is bit-for-bit unchanged. Rule #1 clean: "
                             "no strategy/regime/band/sizing knob touched. READ-ONLY "
                             "throughout; NOTHING transmitted; +holding_class/engine/"
                             "crm_outofspec/dailyreport/dashboard test matrix. "
                             "(2) FA-BLOCK LANE TWO-PHASE CASH GATE (conductor #64). PROBLEM: the "
                             "block lane placed its routes in ARBITRARY order and never read "
                             "back realized cash — the exact shape of the 2026-07-28 incident "
                             "(a sell was CANCELLED and the buys fired anyway, committing $115k "
                             "against ~$76k). FIX: live_fa_block_execute now partitions routes "
                             "SELL-before-BUY, places every sell block and WAITS it to terminal "
                             "state (bounded by PHASE_TERMINAL_TIMEOUT_SEC; a straggler is "
                             "CANCELLED and reported LOUDLY so a still-working sell is never "
                             "alive across the cash read), waits CASH_SETTLE_SEC, RE-READS "
                             "TotalCashValue per SUB-ACCOUNT off a fresh accountSummary, then "
                             "RE-SIZES the buy blocks — both the block quantity AND the group's "
                             "ContractsOrShares split — so each sub-account buys only what its "
                             "OWN realized cash covers with CASH_SAFETY_BUFFER_PCT applied, and "
                             "only then places them. Cash is NEVER netted across accounts and "
                             "quantities are never rounded up. This is safe_execute's PROVEN "
                             "gate, IMPORTED not copied (CASH_SAFETY_BUFFER_PCT, "
                             "CASH_SETTLE_SEC, PHASE_TERMINAL_TIMEOUT_SEC, _TERMINAL_STATUSES, "
                             "_total_cash_value, _scale_buys_to_cash, _report_phase, plus "
                             "s0_live.filter_account_summary) so the block lane and the "
                             "per-account lane can NEVER drift on the cash discipline; the only "
                             "genuinely new logic is re-sizing a SPLIT (a block is one order "
                             "shared by many accounts, so safe_execute's per-account leg scaler "
                             "cannot be called directly) and it carries the same hard "
                             "per-account invariant assert (buy notional <= cash*(1-buffer)). "
                             "Unreadable cash FAILS CLOSED — that account contributes ZERO to "
                             "the buy phase — as does an unpriceable block. New "
                             "uninvested_proceeds_report: any account that RAISED proceeds but "
                             "ends the run sitting in cash is reported loudly and "
                             "machine-readably (CASH_UNREADABLE / NO_BUY_ROUTE / "
                             "BUY_SHORT_OF_REALIZED_CASH, measured against INTENDED buy "
                             "notional so a pre-existing cash sleeve doesn't cry wolf), so a "
                             "sold-but-not-reinvested account is never silently left behind. A "
                             "route whose side is neither BUY nor SELL REFUSES THE WHOLE RUN "
                             "before the backup, before any replaceFA and before any order — "
                             "phase order is never guessed. PREVIEW is unchanged in kind: it "
                             "prints the full phase plan with ZERO broker interaction (no "
                             "accountSummary read, no cash gate, no writes). Existing summary "
                             "keys are unchanged for existing callers; new keys are additive. "
                             "Order-affecting (changes buy quantities and the group split on "
                             "the armed block lane). Rule #1 clean: no strategy/regime/band/"
                             "sizing knob touched. NOTHING transmitted (fully mocked); +phase/"
                             "cash-gate/uninvested test matrix."),
    ("0.32.0", "2026-08-05", "FA-GROUP BLOCK EXECUTOR — the advisor-account/FA-group "
                             "block-execution path (owner's chosen architecture: advisor master "
                             "+ FA groups + block orders, NOT per-account-direct). New "
                             "paperbot/live_fa_block_execute.py, a PORT of the proven paper "
                             "FA-block flow in rebalance_execute.py, COMPOSED from existing "
                             "primitives (rebalance_engine.build_plan -> per-account target "
                             "shares + fa_block routes; rebalance_run.resolve_tier_groups "
                             "fail-closed; rebalance_execute.backup_fa_groups + "
                             "set_group_contracts_or_shares; order_router.build_fa_block/place/"
                             "transmit_guard/already_present; safe_execute.armed_session/"
                             "account_wall_ok/_margin_preflight_ok + connections.gateway_probe). "
                             "PREVIEW BY DEFAULT: sizes each enrolled account, resolves tier "
                             "groups read-only, and surfaces the EXACT would-write groups-XML "
                             "DIFF per fa_block route for human review, builds the block "
                             "(build-only), transmits nothing + writes no FA config. Transmit + "
                             "replaceFA require ALL of: the exact --arm-i-understand token, "
                             "config.READONLY/DRY_RUN flipped False in-process (restored in a "
                             "finally via armed_session; on-disk defaults stay True/True), AND a "
                             "physically-armed gateway (Read-Only API OFF, measured live via the "
                             "shared zero-transmission probe on the TARGET port). The account "
                             "wall is applied over the WHOLE block split (every member enrolled "
                             "or the block is refused); a self-computed per-account margin "
                             "pre-flight over the split is the whatIf SUBSTITUTE. Group write is "
                             "guarded by a MANDATORY groups-XML backup before any replaceFA, "
                             "preserve-other-groups, and fail-closed-on-blank/missing-group/"
                             "missing-accts. The TARGET gateway/login is a PARAMETER "
                             "(TargetGateway): built + VALIDATED against the PAPER advisor master "
                             "DF8922141 (port 4002, subs DU8922142-146, connect pinned to a DU "
                             "sub, readonly=False only on the armed path); a documented (inactive) "
                             "LIVE_GATEWAY is a one-line config flip once the owner provisions the "
                             "live advisor login (the 4003 login is NOT an advisor account today: "
                             "2 direct accounts, no master, requestFA times out — tested "
                             "2026-08-05). Ships WITHOUT whatIf (FA_BLOCK_WHATIF_ENABLED=False "
                             "stub; whatIf on a block hangs). NEVER whatIf a block. New clientIds "
                             "62 (paperbot_live_fa_block_exec, paper build consumer) + 63 "
                             "(live_fa_block_exec, RESERVED for the future 4003 target). "
                             "Refactor (behavior-preserving): rebalance_execute."
                             "set_group_contracts_or_shares now delegates the XML mutation to a "
                             "new PURE compute_group_contracts_or_shares_xml(current_xml, group, "
                             "split) so the previewed DIFF is byte-identical to what the armed "
                             "writer replaceFA's, and preserve-others/fail-closed is testable "
                             "offline — rebalance_execute's own tests stay green (41), proving the "
                             "write output is unchanged. Order-affecting (introduces a new "
                             "transmit-capable FA-block routing/allocation path), STAGE 1 = "
                             "preview + group-write + tests only; NOTHING transmitted, no real "
                             "order placed, 4003 NOT pointed at as an advisor account. Rule #1 "
                             "clean: no strategy/regime/band/sizing knob touched. +27 offline "
                             "tests; full paperbot suite green (601 pass; the 2 pre-existing "
                             "test_s0_live_pilot_run failures are unrelated)."),
    ("0.31.0", "2026-08-05", "FAIL-SAFE NAV/holdings mismatch pre-arm exclusion in the "
                             "whole-book out-of-spec scan (crm_outofspec). PROBLEM (surfaced by "
                             "the 2026-08-05 stale-NAV investigation): a batch could in principle "
                             "be sized off an account whose recorded NAV (roster total_value) "
                             "wildly disagrees with the holdings it should reflect. Live example: "
                             "two departed 'APS Ventures, LLC' accounts (U7349572, U7349597) "
                             "whose positions transferred out — total_value correctly went to ~$0 "
                             "($1,073 / $221) while their last holdings_snapshot (2026-07-27, "
                             "~$836,622 / ~$195,156) is now STALE because IBKR stopped reporting "
                             "the closed accounts. FIX (crm_outofspec.account_inputs_from_roster): "
                             "before the net_liq fallback, compare a PRESENT total_value against "
                             "SUM(holdings.market_value)+cash_balance and, when total_value is "
                             "materially BELOW it (< 50% of holdings+cash, reference > $1k), hold "
                             "the account OUT of the engine entirely (new `excluded` return; "
                             "surfaced by scan_out_of_spec as excluded/n_excluded with reason 'NAV/"
                             "holdings mismatch — manual review (possible closed/departed account "
                             "or data issue)'). It is a FAIL-SAFE EXCLUSION, not a heal/re-sync "
                             "claim — it asserts nothing about which side is right, only that the "
                             "two disagree too much to size safely; a human reviews it (these two "
                             "are departed and will be archived separately — NOT healed). "
                             "Deliberately ONE-SIDED: total_value > holdings is the NORMAL cash "
                             "cushion (holdings_snapshots excludes cash) and is NEVER flagged — "
                             "verified on live data 2026-08-05 (departed ratio ~0.001 vs cash-heavy "
                             "~1.00; a 0.5 cut cleanly separates them). Gated on total_value being "
                             "PRESENT so a genuinely-absent NAV still falls back to holdings. "
                             "cash_balance is not on the roster view today, so the reference "
                             "degrades to holdings-alone — CONSERVATIVE (can only ever flag a "
                             "strict subset, never a healthy account) and it consumes cash "
                             "automatically once the view exposes it. Order-affecting: changes "
                             "which accounts are eligible for an armed batch (U7349572/U7349597 are "
                             "now EXCLUDED from sizing). Rule #1 clean: no strategy/regime/band/"
                             "sizing knob touched. READ-ONLY throughout; NOTHING transmitted; "
                             "+7 tests (departed excluded, cash-heavy/normal not flagged, "
                             "cash-in-row, present-zero-vs-stale-holdings, sub-floor, scan surfaces "
                             "excluded). Full paperbot suite green (574 pass; the 2 pre-existing "
                             "test_s0_live_pilot_run failures are unrelated)."),
    ("0.30.0", "2026-08-05", "BOND valuation + leg-emission fix in the whole-book out-of-spec "
                             "scan (crm_outofspec). BUG (flagged by the whole-book out-of-spec "
                             "scan, then CONFIRMED against live IBKR data 2026-08-05 for U7349974/"
                             "U7349657/U7552751/U7552750/U7333246): the pure engine values every "
                             "holding as qty*mark, but IBKR carries an individual BOND with a "
                             "FACE-value quantity (e.g. 10000) and a per-100 (percent-of-par) "
                             "price (e.g. 100.146), so a bond was valued ~100x too high "
                             "(10000*100.146 = 1,001,462 vs the true 10,014.63 = qty*mark/100, "
                             "which equals IBKR's own reported market_value exactly), inflating "
                             "NAV/drift, and the engine can't emit a placeable equity order for a "
                             "face-value/per-100 CUSIP. FIX (crm_outofspec.account_inputs_from_"
                             "roster, the CRM->engine assembly layer that alone sees "
                             "asset_category): rows with asset_category=='BOND' are (a) valued "
                             "CORRECTLY as qty*mark/100 into the account's holdings value/NAV, and "
                             "(b) held OUT of the engine's positions/prices — so the pure engine "
                             "never sees a bond, never inflates the account's valuation, and never "
                             "emits a broken bond leg; they are returned on `bonds` and surfaced by "
                             "verdicts_from_plans as n_bonds/bonds with action 'manual liquidation "
                             "required (bond)' (S0 Growth holds no individual bonds — all "
                             "sells-to-exit, done by a human), making a bond-holding account "
                             "out_of_spec. The account's NON-bond legs still size against the "
                             "corrected full NAV. Control-plane page surfaces a Bonds (manual) "
                             "column + a manual-liquidation expander. The rebalance_engine and "
                             "reconcile are UNTOUCHED; the crm_rebalance sleeve path already "
                             "excludes non-STK instruments. Order-affecting (corrects NAV-driven "
                             "sizing + which positions become legs) but NON-bond accounts are "
                             "byte-identical — proven by a characterization test (ETF-only plan "
                             "identical with/without the new `bonds` key) plus new tests (bond "
                             "valued /100 not *100, bond legs excluded + flagged, mixed bond+ETF "
                             "sizes the ETF part off corrected NAV). Rule #1 clean: no strategy/"
                             "regime/band/sizing knob touched. READ-ONLY throughout; NOTHING "
                             "transmitted."),
    ("0.29.0", "2026-07-31", "safe_execute: DEPLOY-vs-REBALANCE purpose + self-computed "
                             "per-account MARGIN pre-flight (#57). (1) ExecutionRequest gains a "
                             "trailing `purpose` field (default PURPOSE_DEPLOY): DEPLOY is the "
                             "first-deploy lane that REQUIRES conform to transmit (liquidate "
                             "aliens + fully conform); REBALANCE is the ongoing lane that does "
                             "NOT require conform (trims to target, leaves non-target holdings in "
                             "aliens_left). execute_plan derives conform_required=(purpose==DEPLOY) "
                             "and purpose_ok=(conform if conform_required else True), and the two "
                             "connection-dependent guards + the permit now key on purpose_ok "
                             "instead of a hardcoded conform. For purpose=DEPLOY purpose_ok==conform, "
                             "so the S0 single-account DEPLOY path is BYTE-IDENTICAL — proven by the "
                             "unchanged DEPLOY suite (armed/preview/caps/idempotency) staying green "
                             "plus explicit conform-false-blocks / conform-true-clean parity tests. "
                             "This fixes the bug where an ongoing REBALANCE (conform=False by "
                             "nature) could NEVER transmit. crm_execute now tags its multi-account "
                             "rebalance requests purpose=REBALANCE (still PREVIEW-only here). (2) New "
                             "_margin_preflight_ok gate (reuses s4_risk.margin_preflight) runs right "
                             "after the buying-power check on the armed lane: computes intended "
                             "post-trade risk exposure = plan.investable/NAV (the strategy's own "
                             "risk ceiling; ~0.985 for a fully-invested S0 book, structurally <=1.0 "
                             "since investable can't exceed NAV without borrow) against a hard "
                             "leverage_cap=1.0 (S0/rebalance accounts are UNLEVERED). HARD INVARIANT: "
                             "for exposure<=1.0 it adds ZERO reasons on ANY account type — incl. the "
                             "trust U14438624 (AccountType=TRUST, BuyingPower>NetLiq) — and fails "
                             "OPEN on a thin/empty summary (unlevered branch never reads BP/type), "
                             "matching _buying_power_ok. It fails CLOSED and refuses ONLY a genuinely "
                             "levered (exposure>1.0) request on a cash/unknown/thin-BP account. The "
                             "gate is equal-or-stricter: guarded by `not reasons`, it can only ADD a "
                             "block, never clear one. Order-affecting (adds one new transmit lane + "
                             "one new blocking gate) but S0 DEPLOY behavior unchanged. Rule #1 clean: "
                             "no strategy/regime/band/sizing value touched. NOTHING transmitted "
                             "(fully mocked; full paperbot suite green)."),
    ("0.28.1", "2026-07-30", "s0_live_exec adopts the shared connections.gateway_probe "
                             "(folds the third probe copy; closes #70); read-only, "
                             "behavior-preserving. s0_live_exec._probe_gateway_readonly is now a "
                             "thin same-named wrapper over gateway_probe.probe_api_readonly "
                             "(port=4003, fail-closed True) — the SAME delegation "
                             "safe_execute._probe_gateway_readonly uses — so the "
                             "cancel-a-fabricated-order idiom lives in ONE place and the tiny-test "
                             "executor's arm gate keeps its exact caller/signature. Now-unused "
                             "threading/time imports dropped. Zero-transmission: full paperbot "
                             "suite green (523; zero regressions). Rule #1 clean: no strategy/"
                             "regime/band/sizing value touched. NOTHING transmitted."),
    ("0.28.0", "2026-07-30", "consolidate the two gateway read-only probes into one "
                             "port-parameterized probe_api_readonly (arming 4002 + safe_execute "
                             "4003); zero-transmission technique unchanged; final §2.2 item of "
                             "#64. The cancel-a-fabricated-order idiom now lives ONCE in "
                             "connections/gateway_probe.py (neutral low layer -> no import "
                             "cycle; connections never imports paperbot). arming.probe_api_readonly "
                             "keeps its exact public contract — opens its own paper-4002 "
                             "connection (clientId 39, readonly=False), delegates the technique, "
                             "and still RAISES on no-signal (raise_on_indeterminate=True) so "
                             "disarm's verify never silently claims 'locked' on an unmeasurable "
                             "line. safe_execute._probe_gateway_readonly is now a thin same-named "
                             "wrapper (port=4003, fail-closed True) so execute_plan + the "
                             "Control-Plane gateway_arm_probe keep importing it unchanged. "
                             "Order-affecting (touches the live-trade arm probe idiom) but "
                             "behavior-preserving: full paperbot suite green (516 -> 523, +7 "
                             "shared-probe tests; zero regressions). Rule #1 clean: no strategy/"
                             "regime/band/sizing value touched. NOTHING transmitted."),
    ("0.27.1", "2026-07-30", "rebalance_execute adopts safe_execute.armed_session "
                             "(flip-and-restore-in-finally; flags no longer leak to process "
                             "exit); gateway_lock + FA-backup unchanged; safety banner moved "
                             "into execute_armed so an armed run's banner reflects the "
                             "flipped/transmit-capable state (it read pre-flip flags after the "
                             "Step-2 flip relocation); Step 2 of arm-gate unification #64"),
    ("0.27.0", "2026-07-30", "shared armed_session arm-gate context manager; deploy engine "
                             "(execute_plan) adopts it; behavior-preserving (flags still "
                             "flip-and-restore); Step 1 of arm-gate unification #64"),
    ("0.26.0", "2026-07-30", "SHARED SAFE EXECUTION ENGINE extracted (Production Rebalance "
                             "Control Plane, conductor #64/#66, spec §2/§7 Phase 2). New "
                             "paperbot/safe_execute.py exposes ONE reusable primitive "
                             "execute_plan(request, *, mode, ib=None) -> ExecutionResult (plus "
                             "ExecutionRequest/ExecutionResult/ExecutionCaps dataclasses) that "
                             "encapsulates everything AFTER planning: the ordered leg build, the "
                             "full fail-closed pre-flight gate, and the two-phase cash-gated "
                             "transmit. The proven s0_live_deploy execution logic was MOVED "
                             "VERBATIM (not rewritten) into it — build_deploy_legs, "
                             "_scale_buys_to_cash, _leg_cap/_more_aggressive_cap, "
                             "_working_order_present, _transmit_phase, _report_phase, the "
                             "preflight gate assembly (arm token, READONLY/DRY_RUN false, armed, "
                             "gateway write-enabled via probe, kill-switch absent, account in "
                             "allowed_accounts, fresh+sane quotes, whole-share, per-order + "
                             "total caps, price guard, position-based idempotency), the "
                             "two-phase cash-gated orchestration (sells -> re-read realized "
                             "TotalCashValue -> buys sized to that cash with the hard invariant "
                             "buy_notional <= cash*(1-buffer)), the notional caps, and the "
                             "gateway read-only probe. modes: PREVIEW (default; transmit "
                             "nothing) / ARMED (flip-and-restore-in-finally two-phase transmit "
                             "iff the full gate passes) — no third mode. s0_live_deploy is now a "
                             "THIN CALLER: it keeps its CLI, connect-lane choice, account/"
                             "positions/quotes read, plan sizing, safety banner, and EXACT "
                             "stdout sections [1]-[9], builds an ExecutionRequest, and delegates "
                             "the gate+legs+transmit to the engine; every moved name is "
                             "re-exported so existing imports/tests keep working unchanged. "
                             "Order-affecting: it is an extraction of the real-money transmit "
                             "path — PROVEN byte-for-byte behavior-preserving by (a) the ENTIRE "
                             "paperbot suite still green (493 -> 506 with +13 engine tests; zero "
                             "regressions) and (b) a characterization harness diffing old-vs-new "
                             "stdout + rc + placed-order set across 6 scenarios (preview / armed "
                             "transmit / armed-without-conform / short-cash / over-investable / "
                             "two-phase sells-before-buys) — IDENTICAL modulo the wall-clock "
                             "run_id. Only import-path test edits (probe + tuning constants "
                             "moved module; assertions unchanged). Scope: does NOT yet unify the "
                             "rebalance_execute arm gate or consolidate arming.probe (separate "
                             "later increments — TODO(safe-execute) markers left). Rule #1 "
                             "clean: no strategy/regime/band/sizing value touched. NOTHING "
                             "transmitted building or testing it (fully mocked)."),
    ("0.25.1", "2026-07-29", "s0_live_deploy: removed the --model-clear affirmation gate and "
                             "IBKR model-overlay detection — model divestment is handled "
                             "manually by the account owner (explicit direction; the check was "
                             "unwanted friction). All other deploy safety gates unchanged "
                             "(single-account wall U14438624, --arm-i-understand + --conform "
                             "both required, live gateway read-only arming probe, two-phase "
                             "cash-gate, straggler re-price, per-run ref, <=50% NetLiq per-order "
                             "cap, total-buy<=investable, buying-power check, kill switch, "
                             "in-process READONLY/DRY_RUN flip in try/finally, preview-by-"
                             "default). Rule #1 clean: no strategy/regime/band/sizing value "
                             "touched. NOTHING transmitted editing or testing it (fully mocked)."),
    ("0.25.0", "2026-07-28", "s0_live_deploy REBUILT SAFER after the 2026-07-28 first live "
                             "deploy went ~$40k NEGATIVE (conductor #63 / log #145). Four "
                             "order-affecting fixes: (1) TWO-PHASE CASH-GATED execution — "
                             "phase 1 transmits ONLY the sells (plan sells + conform ALIEN "
                             "liquidations) and WAITS for terminal state; between phases it "
                             "RE-READS live TotalCashValue (realized cash, never the plan's "
                             "expected proceeds); phase 2 sizes buys to that ACTUAL cash with "
                             "a hard invariant (total BUY notional <= cash*(1-1% buffer)), "
                             "whole-share scaling/skipping buys if a sell fell short — buying "
                             "against money that hasn't arrived is now structurally impossible "
                             "(root cause: BUCK's ~$40k sell cancelled, so $115k of buys "
                             "committed against ~$76k). (2) STRAGGLER RE-PRICE — an unfilled "
                             "leg is cancelled + re-placed at a more aggressive marketable "
                             "limit toward the far touch, up to 3 attempts, re-placing only "
                             "the unfilled remainder; a leg still unfilled at the cap is left "
                             "cancelled and reported LOUDLY, never silently dropped. (3) "
                             "PER-RUN order ref — the deploy ref now carries a per-run stamp "
                             "so a fresh run's legs never match a prior run's fills (fixes the "
                             "re-fire false-block where a bought-then-sold SPY looked 'already "
                             "done'); correctness comes from the engine's delta-vs-positions, "
                             "and double-submit protection is kept by a symbol+side check "
                             "against currently WORKING open orders (ref-independent). (4) "
                             "MODEL PRE-CHECK — a new REQUIRED --model-clear human affirmation "
                             "gate (the account was still in IBKR model 'Main', invisible to "
                             "our sub-account read-only lane); armed deploy hard-stops without "
                             "it, plus a best-effort master-level detection that refuses on a "
                             "positively-detected non-empty modelCode. All prior gates intact "
                             "(arm token, conform, kill switch, single-account U14438624 wall, "
                             "<=50% NetLiq per-order cap, total-buy<=investable, price guard, "
                             "gateway-armed probe, in-process READONLY/DRY_RUN flip in "
                             "try/finally, preview-by-default). Rule #1 clean: no strategy/"
                             "regime/band/sizing value touched — execution-safety only. "
                             "NOTHING transmitted building or testing it (fully mocked). "
                             "+two-phase/re-price/per-run-ref/model-gate test matrix."),
    ("0.24.1", "2026-07-28", "s0_live_deploy: give deploy orders their own :deploy ref "
                             "namespace so the dedup gate no longer false-blocks on a "
                             "collision with the one-off tiny-test (or normal rebalances); "
                             "re-send protection unchanged."),
    ("0.24.0", "2026-07-28", "add s0_live_deploy.py -- full-account DEPLOY executor with "
                             "explicit --conform mode (liquidates non-S0 holdings to deploy "
                             "any book into the S0 GROWTH-tier target); armed+conform gated, "
                             "whole-share, price-guarded, dedup, notional-capped (total BUY "
                             "<= investable; no single order > 50% NetLiq), sells-before-buys, "
                             "pinned to U14438624."),
    ("0.23.0", "2026-07-28", "s0_live_exec retargeted to trust account U14438624 "
                             "(Andrew-authorized; U5721712 PDT-blocked); single-account wall "
                             "repointed, all other caps unchanged."),
    ("0.22.0", "2026-07-27", "S0 tiny-test REAL-TRANSMISSION path (Andrew-authorized): new "
                             "s0_live_exec.py — the desk's FIRST order path that can transmit a "
                             "real order, on the funded individual live TEST account U5721712 on "
                             "the Live-Trade Gateway (port 4003; NEVER the trust account "
                             "U14438624). It reuses the read-only pilot verbatim for WHAT to trade "
                             "(strategy_target.current_target -> rebalance_engine.plan_account "
                             "against U5721712 with live prices, filtered to the individual "
                             "account) and adds ONE capped, marketable LIMIT built with "
                             "order_router.build_marketable_limit and placed with "
                             "order_router.place(armed=True) — both UNCHANGED. PREVIEW BY DEFAULT: "
                             "with no flag it sizes, picks + builds the candidate, prints 'WOULD "
                             "TRANSMIT', and sends nothing. Transmission requires ALL of: the exact "
                             "--arm-i-understand token (sets armed=True; never defaulted), no "
                             "kill-switch sentinel (AUTOTRADE_DISABLED), target account EXACTLY "
                             "U5721712, a BUY of a whitelisted symbol (USFR only), qty <= "
                             "MAX_TEST_SHARES (1) AND shares*limit <= MAX_TEST_NOTIONAL ($150), 1 "
                             "order/run, the Gateway physically armed (Read-Only API toggle off, "
                             "measured live via arming's zero-transmission cancel-a-fabricated-"
                             "order probe mirrored against the open 4003 connection — arming.py "
                             "itself is paper-4002-only and can't be pointed at 4003 without "
                             "editing it), and the pre-transmit dedup gate "
                             "(order_router.already_present) FRESH. Miss any -> preview, prints "
                             "WHY. NEVER a market order. No auto-arm, nothing scheduled; a human "
                             "arms + fires. New connect_s0_live_armed() in s0_live.py (readonly="
                             "False, clientId s0_live_exec=58) is the ONLY armed lane; the "
                             "read-only pilot lane is untouched. Order-affecting (introduces real "
                             "transmission). Rule #1 clean: reuses strategy_target / "
                             "rebalance_engine / order_router / arming idiom / config UNCHANGED — "
                             "no strategy/regime/band/sizing value touched. NOTHING was transmitted "
                             "building or testing it (built + tested preview-only, fully mocked). "
                             "+offline test matrix (test_s0_live_exec.py)."),
    ("0.21.0", "2026-07-27", "S0 live-pilot PREVIEW path (conductor #3/#41, Slice 2): new "
                             "s0_live_pilot_run.py — the single-account analog of the paper FA "
                             "morning pipeline, pointed at a REAL funded account. It reads the "
                             "individual live-trading TEST account U5721712 on the Live-Trade "
                             "gateway (port 4003 — the same gateway S8 pilots on; S8 uses the "
                             "TRUST account U14438624 under the same login, never this one) via "
                             "the read-only s0_live lane (connect_s0_live -> "
                             "ibkr_live_trade.connect(readonly=True); readonly=False is never "
                             "passed), filters every read to U5721712 (never the trust account), "
                             "computes S0's target with the shared brain (strategy_target."
                             "current_target -> backtester run_backtest), fetches real-time "
                             "reference prices on 4003, sizes the target against the real account "
                             "with the UNCHANGED rebalance_engine.plan_account (config."
                             "REBALANCE_BAND_PCT band; reserve 0 since U5721712 is not in "
                             "cashflows.SCHEDULE), and prints + emails 'WOULD HAVE TRANSMITTED' "
                             "via the shared dailyreport mailer/status. ZERO-TRANSMIT with two "
                             "independent walls: (1) the connection is read-only, and (2) the "
                             "runner is BUILD/PREVIEW-ONLY — there is NO arm path and NO transmit "
                             "code in the file at all (imports no order_router; never calls "
                             "place()/place_laddered()/ib.placeOrder()/arming). MANUAL, on-demand "
                             "for now — deliberately NOT scheduled; the first pilot cycles are "
                             "human-reviewed before any automation. Order-affecting (new order-"
                             "sizing preview path against a real account), though it transmits "
                             "nothing. Rule #1 clean: reuses strategy_target / rebalance_engine / "
                             "reconcile / cashflows / config UNCHANGED — no strategy/regime/band/"
                             "sizing value touched. +offline test matrix "
                             "(test_s0_live_pilot_run.py)."),
    ("0.20.0", "2026-07-24", "Per-run margin/buying-power monitoring (conductor #26). New "
                             "margin_monitor.py snapshots accountSummary immediately before "
                             "and after the ARMED transmit in order_router.place()/"
                             "place_laddered(), diffs BuyingPower/ExcessLiquidity/"
                             "InitMarginReq/MaintMarginReq/AvailableFunds, and writes a "
                             "durable kind='margin_impact' record to the audit ledger "
                             "(runs.jsonl) for after-the-fact intraday margin review — "
                             "closing the gap where s4_risk.margin_preflight()'s result was "
                             "only printed, never persisted. Capture is per-RUN not "
                             "per-single-order (a per-leg accountSummary round-trip would add "
                             "latency and race fills) and fully fail-soft: any summary read "
                             "error degrades to margin=None and never blocks or alters order "
                             "transmission. nav_history.py daily CSV now also carries "
                             "buying_power + excess_liquidity columns (read from "
                             "accountValues the S0 monitor already pulls — no extra broker "
                             "call; pre-existing rows backfilled NaN) for an EOD margin time "
                             "series. Order-path-adjacent OBSERVABILITY only — no routing/"
                             "sizing/allocation/frozen-config change; S0 is cash-only so "
                             "nothing fires there today, this is the prerequisite for S8 "
                             "options going live in paperbot."),
    ("0.19.0", "2026-07-24", "flatten_accounts.py SAFETY REWORK (conductor #51 — was "
                             "HIGH-severity DO-NOT-RUN). The one-shot liquidation tool was "
                             "load-bearing-unsafe: it hardcoded all five DU sub-accounts, "
                             "took no args, hardcoded order.transmit=True, and RUNNING THE "
                             "MODULE WAS THE SWEEP — so a launch would liquidate the entire "
                             "live S0 book. It also priced EVERY position via a Stock() "
                             "quote, so an OPTION position was priced at the UNDERLYING and "
                             "legged out NAKED. Reworked: (a) argparse with REQUIRED "
                             "--accounts AND --symbols/--conids allowlists (no default) + a "
                             "hard guard at the top of main() that exits non-zero and never "
                             "connects if either is absent; --execute is now the ONLY thing "
                             "that transmits, DRY-RUN is the default (prints intended "
                             "orders, places nothing). (b) instrument-aware: refuses any "
                             "non-STK secType (OPT/FUT/BAG/…) with a loud warning and marks "
                             "the run non-flat — never prices off the underlying, never "
                             "legs out an option (combo-close left as an explicit TODO). "
                             "(c) flatten now validates its OWN limit price (rejects NaN / "
                             "non-finite / <=0 via math.isfinite — the old `if not ref:` let "
                             "NaN through) instead of trusting live_quotes upstream. (d) "
                             "cancels any still-working order before disconnect, and a "
                             "non-flat reconcile is now a HARD FAILURE (non-zero exit), not "
                             "a printed note. (e) copies the live position's contract before "
                             "forcing exchange=SMART instead of mutating it in place. "
                             "Order-affecting: changes which accounts/instruments the "
                             "liquidation tool will act on and how it routes/cancels — a "
                             "flatten can no longer default to 'everything in five "
                             "accounts' or send a mispriced naked option order. PAPER only; "
                             "+13-case offline test matrix (test_flatten_accounts.py). Rule "
                             "#1 clean: no strategy/regime/band/sizing value touched."),
    ("0.18.0", "2026-07-20", "S0 live-account reconciliation / corp-action guard (armed-live "
                             "prereq #3). reconcile.reconcile() gains an opt-in `universe` "
                             "param (the strategy's tradeable symbols, from a new "
                             "strategy.universe() accessor on StrategyBase/AdaptiveAllWeather "
                             "== set(strategies.config.ALL_TICKERS), verified a superset of "
                             "every sleeve's output) that SPLITS the single UNTRACKED status "
                             "into ROTATE_OUT (held, weight 0, symbol IN universe, int(sh)>=1 "
                             "-> a known ticker the model dropped -> SELL, normal rebalancing), "
                             "ALIEN (symbol NOT in universe / whitelist / cash -> corp-action "
                             "or manual holding -> REVIEW, never auto-traded), FRACTIONAL "
                             "(int(sh)==0, sh!=0 -> DRIP stub, suppressed from the band so it "
                             "stops false-paging nightly), and SWEEP (whitelisted cash/MMF). "
                             "`universe is None` (backtester + every existing caller) "
                             "reproduces today's UNTRACKED classification bit-for-bit. "
                             "rebalance_engine.band_breached now breaches on ROTATE_OUT (and "
                             "genuine trade-size drift) but NEVER on ALIEN/FRACTIONAL/SWEEP; "
                             "plan_account emits a sell delta for ROTATE_OUT, NOTHING for "
                             "ALIEN/FRACTIONAL/SWEEP, and collects ALIEN lines onto "
                             "AccountPlan.alien_lines. universe threaded through "
                             "plan_accounts/build_plan. New config.SWEEP_WHITELIST (default "
                             "EMPTY, discover-then-whitelist) extends the investable.CASH_SYMBOL "
                             "anchor. nightly_monitor_run + morning_execute_run emit a distinct "
                             "CORP-ACTION / UNTRACKED REVIEW alert on any ALIEN holding without "
                             "blocking legitimate ROTATE_OUT/DRIFTED/MISSING rebalancing (an "
                             "alien-only cycle is 'needs review', not a false 'band breach, no "
                             "routes' page). recon_report shows the new statuses + a quiet "
                             "FRACTIONAL/SWEEP line + an ALIEN review section. Order-affecting: "
                             "changes which held symbols the engine will and won't auto-trade "
                             "(a spinoff/rename is no longer churned into a taxable round-trip; "
                             "a known dropped ticker still sells). Behavior-identical on paper/"
                             "backtester when no universe is passed; +live-reconciliation test "
                             "matrix. Rule #1 clean: no frozen strategy/regime/band/sizing value "
                             "changed (REBALANCE_BAND_PCT, cash_reserve_pct, ALL_TICKERS et al. "
                             "untouched) — this is reconciliation plumbing + a safety gate. "
                             "Still zero-transmit (PILOT_MODE/READONLY/DRY_RUN unchanged)."),
    ("0.17.0", "2026-07-20", "S0 order idempotency: the deterministic orderRef is now "
                             "ENFORCED, not just documented. New pre-transmit dedup gate "
                             "order_router.already_present() reads BROKER TRUTH "
                             "(reqAllOpenOrders working refs + reqExecutions today's fills "
                             "per orderRef; NEVER whatIfOrder) and a new append-only "
                             "transmit_journal.py (ATTEMPTING/SENT/CYCLE_COMPLETE JSONL "
                             "under STATE_DIR, off Drive) to classify each leg FRESH/"
                             "WORKING/COMPLETE/PARTIAL/UNKNOWN; only FRESH transmits, "
                             "everything else transmits nothing (PARTIAL/UNKNOWN also "
                             "alert). Fails CLOSED: any broker-read error -> UNKNOWN. Wired "
                             "per-leg at the top of the transmit branch of place() and "
                             "before rung 1 of place_laddered(). morning_execute_run path "
                             "[4] now journals ATTEMPTING before / SENT after each leg, "
                             "runs the gate in BOTH pilot (zero-transmit rehearsal, logs "
                             "WOULD SEND/WOULD SKIP) and armed modes, and calls "
                             "ledger.record_run() per cycle (closes the missing per-run "
                             "audit line). Staged-file archive is now a tidy-up, not the "
                             "safety mechanism. Order-affecting (routing/idempotency): a "
                             "crash-resume, retry, manual re-run, or stacked ladder no "
                             "longer double-sends what the broker already has working or "
                             "filled. Still zero-transmit today (PILOT_MODE/READONLY/"
                             "DRY_RUN unchanged); +order-idempotency test matrix."),
    ("0.16.0", "2026-07-17", "S8 live-pilot margin preflight fixed for the real "
                             "live-trade account (port 4003, U14438624). Two order-gating "
                             "corrections, both found by reading the live account: (1) the "
                             "login exposes TWO managed accounts (trust U14438624 + "
                             "individual U5721712) plus an 'All' aggregate, so s8_runner "
                             "now filters ib.accountSummary() to s8_config.ACCOUNT before "
                             "the preflight (new filter_account_summary; fail-closed refuse "
                             "if the target account is absent) -- previously _summary_map's "
                             "last-write-wins collapse let the preflight read the WRONG "
                             "account's numbers. (2) s4_risk.account_is_margin now also "
                             "treats BuyingPower > NetLiquidation as margin: IBKR reports "
                             "AccountType='TRUST'/'INDIVIDUAL' (legal-entity label, no "
                             "'MARGIN' token) for this account, which the string-only test "
                             "wrongly refused; BP>NLV is impossible on a cash account, a "
                             "definitive margin tell with no tunable threshold (rule #1 "
                             "clean). s8_risk passes BuyingPower/NetLiquidation into the "
                             "check; S4's own call stays string-only (unchanged). "
                             "Order-affecting: fixes whether an S8 spread entry is approved "
                             "by the margin gate. Still zero-transmit (PILOT_MODE=True, "
                             "connect readonly=True, gateway ReadOnlyApi=yes); +6 tests."),
    ("0.15.0", "2026-07-09", "DU8922144 and DU8922146 removed from ENROLLMENT "
                             "(Andrew's explicit decision, freeing them up for testing "
                             "other strategies later, e.g. S4/S8). Balanced and Growth "
                             "tiers go from 2-account FA blocks to single-account "
                             "enrollments; rebalance_engine.route_blocks() derives "
                             "routing from live account count per tier, so both tiers "
                             "now route DIRECT automatically (same mechanism that "
                             "already made Conservative/DU8922142 DIRECT) -- no routing "
                             "logic changed, just the ENROLLMENT map. Existing S0 "
                             "positions in the two freed accounts are to be liquidated "
                             "to cash separately, via a deliberate armed PAPER session "
                             "-- NOT part of this change. conductor/ACCOUNT_ALLOCATION.md "
                             "is now the authoritative account->strategy map (update it "
                             "alongside any future ENROLLMENT edit). Order-affecting: "
                             "changes which accounts S0 rebalances and how the two "
                             "affected tiers route."),
    ("0.14.0", "2026-07-09", "Automated nightly-monitor + morning-execute PILOT for "
                             "S0 (Andrew-approved, deliberate exception to the normally "
                             "sacred manual review->arm->transmit gate, discussed and "
                             "scoped in advance): nightly_monitor_run.py (~9:15 PM CT, "
                             "after EodReport) does one bounded-retry (3 attempts / "
                             "~10 min cap, always try/finally-disconnected) gateway "
                             "touch that folds in the old AccountMonitorDaily drift/"
                             "cashflow check, computes S0's current per-tier target, "
                             "and -- only if some enrolled account's plan needs "
                             "rebalancing (rebalance_engine's own band-breach test) AND "
                             "the new rebalance_guard.py passes (ticker allow-list "
                             "against strategies.config.ALL_TICKERS, a named "
                             "MAX_SINGLE_ACCOUNT_TURNOVER_PCT_NAV=0.50 notional cap per "
                             "account, and a regime cross-check against the exact "
                             "market_health_score()/apply_hysteresis() call eod_report's "
                             "9PM email uses) -- STAGES the route list as JSON under "
                             "C:\\TradingDesk-Local\\pending_trades\\YYYY-MM-DD.json. A "
                             "guard failure alerts by email and stages nothing. "
                             "morning_execute_run.py (~8:50 AM CT) makes ZERO gateway "
                             "contact on the common no-staged-file day; otherwise "
                             "checks a kill-switch sentinel "
                             "(C:\\TradingDesk-Local\\AUTOTRADE_DISABLED), re-runs the "
                             "guard defensively against fresh prices/regime (never "
                             "trusts the staged file blindly), and -- because "
                             "PILOT_MODE=True (the hardcoded default; only Andrew may "
                             "flip it to False after reviewing pilot cycles) -- logs and "
                             "emails 'WOULD HAVE TRANSMITTED: ...' instead of calling "
                             "any order-placing code, then archives the staged file. "
                             "The PILOT_MODE=False path (not exercised by this release) "
                             "reuses rebalance_execute.py's own arm/build/place/ladder "
                             "functions verbatim rather than reimplementing them. New "
                             "clientIds 46 (paperbot_nightly_monitor) / 47 "
                             "(paperbot_morning_execute). Existing manual "
                             "rebalance_run.py / rebalance_execute.py CLIs are "
                             "untouched and remain the human-driven path. Two new "
                             "scheduled tasks (nightly monitor retimed onto the "
                             "AccountMonitorDaily slot at 9:15 PM CT; a new "
                             "MorningExecuteDaily task at 8:50 AM CT). Order-affecting: "
                             "introduces a real, if pilot-gated, path to unattended "
                             "paper trade staging; actual transmission remains fully "
                             "gated off by PILOT_MODE=True until a deliberate future "
                             "flip."),
    ("0.13.1", "2026-07-09", "Fixed NaN-as-bearish bug in S0 regime scoring: a "
                             "missing daily price (Tiingo hasn't published yet) "
                             "previously read as a false bearish trend/vol/duration "
                             "break via bare pandas `>` comparisons (NaN>NaN/NaN>0 "
                             "== False, not NaN) instead of unknown/hold-prior-"
                             "state, and pct_change's legacy pad-fill silently "
                             "turned a missing print into a fake flat 0% return. "
                             "Fixed in 4 places: regime.py's trend component "
                             "(_ratio_above_trend, _trend_component's "
                             "trend_above_10m/trend_ret_6m_pos, and the SPY-"
                             "realized-vol stress fallback), regime.py's "
                             "_rolling_slope_positive boolean cast (the docstring's "
                             "NaN-safety promise was undone by the final `> 0` "
                             "cast), volatility.py's realized_vol (was silently "
                             "reading a data-gap night as falsely calm, nudging "
                             "toward MORE equity exposure), and duration.py's "
                             "_above_ma (feeds the live Duration Filter's long-"
                             "Treasury permission/ban rules; now NaN-aware with an "
                             "explicit conservative .fillna(False) at each call "
                             "site, matching gates.is_above's existing convention "
                             "for permission gates). Also fixed eod_report.py: "
                             "'Data as-of' now reports the OLDEST last-real-value "
                             "date across S0's required inputs instead of the "
                             "newest date ANY ticker reached (was able to read "
                             "'fresh' on a night a required ticker's row was NaN), "
                             "and s0_regime's status now inherits s0_data's status "
                             "when it's worse (ok<stale<warn<fail) so the two "
                             "sections can no longer disagree with s0_regime "
                             "looking healthier. Order-affecting: changes S0's "
                             "regime/duration computation on any date with a "
                             "missing input price. Byte-identical when all inputs "
                             "are present (pure NaN-handling fix); backtester + "
                             "paperbot both pick this up automatically via the "
                             "shared strategies package, no separate paperbot "
                             "change needed."),
    ("0.13.0", "2026-07-04", "S4 (SPX vol-control fund) single-account daily REVIEW path, "
                             "shelf-ready. New S4-only siblings (frozen S0 paths untouched): "
                             "s4_strategy_target (shared-brain adapter -> Target, profile "
                             "runtime dial + stale-data guard), s4_sizing (REAL-margin "
                             "leverage sizing: SPY notional = NAV*exposure, exposure may "
                             "exceed 1.0x, borrow leg carried NOT dropped), s4_risk (guard "
                             "permits up to the profile leverage_cap + fail-closed margin "
                             "preflight refusing the >1.0 path on a cash/insufficient-BP "
                             "account), s4_rebalance_run (single-account, gateway-locked, "
                             "place(armed=False)), s4_daily_run (calendar-gated entry point). "
                             "clientIds 44 (paperbot_s4) + 45 (paperbot_s4_exec, reserved). "
                             "Order-affecting: introduces the levered S4 sizing/risk path. "
                             "Transmits nothing; no scheduled task registered; not armed."),
    ("0.12.0", "2026-06-30", "Rebalance run/execute acquire the gateway lock for the whole "
                             "session and wait-then-refuse (naming the holder) if it's held; "
                             "monitor skips on busy. Single-process gateway interlock now "
                             "active (clientIds + lock)."),
    ("0.11.0", "2026-06-30", "Withdrawal earmark fence + sale-raised nudge in monitor core; "
                             "live read-only monitor shell (account_monitor_run.py, clientId "
                             "40) reads SettledCashByDate/fills, persists baselines, runs "
                             "decide(), prints propose-only verdicts. No scheduler yet (held)."),
    ("0.10.0", "2026-06-30", "Deposit-detection core (pure): SettledCashByDate baseline + "
                             "executions cross-check distinguishes external deposit from "
                             "sale-raised/dividend; over-trading guards + per-day debounce; "
                             "emits propose-only DEPOSIT_ARRIVED. No broker connection / no "
                             "scheduler yet (Slice 6b)."),
    ("0.9.0", "2026-06-30", "Per-account monitor brain (account_monitor.py): pure "
                            "Verdict/decide() core — HOLD/REBALANCE/ALERT, propose-only, "
                            "transmits nothing. Withdrawal-coverage + drift + "
                            "untracked-position verdicts; deposit detection deferred to "
                            "Slice 6. Real client schedule data not included (SCHEDULE "
                            "stays empty)."),
    ("0.8.0", "2026-06-30", "Explicit execution-side CASH bucket (~1.5%); risk lines "
                            "reconcile against true model weights; phantom drift removed. "
                            "Backtester untouched; order sizing unchanged."),
    ("0.7.0", "2026-06-30", "Cash buffer 5%->1.5% via single config knob "
                            "(cash_reserve_pct=0.015); risk positions now size ~3.5% of "
                            "NAV closer to model. Order-affecting."),
    ("0.6.0", "2026-06-30", "Consolidated the investable/buffer math into a single leaf "
                            "module investable.py (compute_investable + buffer_pct). The "
                            "5 sites that re-derived (NetLiq-reserve)*(1-cash_reserve) "
                            "inline — rebalance_engine, reconcile, recon_report, "
                            "execution_engine, and the risk_manager reserve threshold — "
                            "now share that one function (rebalance_engine.compute_investable "
                            "kept as a thin re-export). Buffer stays 0.05; proven "
                            "behavior-identical (full paperbot suite green, +8 "
                            "characterization tests pinning the math at 0.05). Slice 1 of "
                            "the account-cashflow build: consolidation only, NO behavior "
                            "change."),
    ("0.5.0", "2026-06-27", "Pure offline multi-account block REBALANCE ENGINE "
                            "(rebalance_engine.py): per-account reserve carve-out + "
                            "explicit integer target shares + per-holding no-trade band; "
                            "same-tier/symbol/side block aggregation with per-account "
                            "ContractsOrShares split (sums to block qty); single-account "
                            "true-up falls back to a DIRECT order. Emits order_router "
                            "inputs as a transmit-free RoutePlan with fa_method='' (the "
                            "group's ContractsOrShares governs; faMethod='NetLiq' is "
                            "rejected, Err 10226). 14 pytest unit tests, all green. No "
                            "broker contact, no order object built, nothing transmitted."),
    ("0.4.0", "2026-06-26", "FA block-order plumbing: read-only FA-config probe "
                            "(fa_probe.py), order_router.build_fa_block(), and a "
                            "what-if-only block validator (fa_block_test.py). No "
                            "transmission, no config writes."),
    ("0.3.0", "2026-06-26", "Multi-account reconciliation + drift report; distribution "
                            "reserve (cashflows); no-trade band; block aggregation with "
                            "per-account allocation split. Read-only."),
    ("0.2.0", "2026-06-26", "Multi-account discovery (accounts.py) + per-tier enrollment "
                            "map. Read-only."),
    ("0.1.0", "2026-06-26", "Single-account dry-run engine: target / diff / risk / build. "
                            "Read-only, transmits nothing."),
]


def banner() -> str:
    """One-line version marker for report headers and logs."""
    return f"paperbot v{VERSION}"


def stamp() -> dict:
    """Version fields to merge into an audit-ledger record (layer 2 of traceability)."""
    return {"paperbot_version": VERSION}
